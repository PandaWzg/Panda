// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("Panda", "for Panda support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("Panda", "enable Panda support", "no");

if (PHP_PANDA != "no") {
	EXTENSION("Panda", "Panda.c");
}

